query posts($location: String) {
  allPosts(address_Startswith: $location) { edges { node {
      id
      to
      signer {id, publicKey}
      link
      address
      karma
      chainLevel
      file {
        content
        contentType
        size
      }
  }}}
}
